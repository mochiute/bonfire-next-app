import Logo from "../logo/logo";
import { HeaderContainer } from "./header-styles";

const Header = () => {
  return (
    <HeaderContainer>
      <Logo />
    </HeaderContainer>
  );
};

export default Header;
